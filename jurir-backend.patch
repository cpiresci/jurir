--- a/src/core/legalPrompts.js
+++ b/src/core/legalPrompts.js
@@ -152,7 +152,7 @@
     id: "civil", area: "Direito Civil",
     specialty: "Contratos, responsabilidade civil, danos morais e materiais",
     providerPref: "sambanova",
-    isCritical: false, maxTokens: 900, temperature: 0.1,
+    isCritical: false, maxTokens: 1800, temperature: 0.1,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado Sênior em Direito Civil com 25 anos em litígios de altíssimo valor e ex-assessor do STJ na 3ª Turma (responsabilidade civil).
@@ -237,7 +237,7 @@
     id: "penal", area: "Direito Penal",
     specialty: "Tipificação de crimes, defesa criminal, penas e recursos",
     providerPref: "sambanova",
-    isCritical: true, maxTokens: 1000, temperature: 0.08,
+    isCritical: true, maxTokens: 2000, temperature: 0.08,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado Criminalista com 22 anos em crimes complexos, ex-Delegado da PGR e especialista em habeas corpus no STF.
@@ -334,7 +334,7 @@
     id: "trabalhista", area: "Direito Trabalhista",
     specialty: "Demissão, verbas rescisórias, horas extras, assédio moral/sexual",
     providerPref: "sambanova",
-    isCritical: false, maxTokens: 1000, temperature: 0.1,
+    isCritical: false, maxTokens: 2000, temperature: 0.1,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado Trabalhista com 20 anos em reclamatórias de alto valor, especialista em Reforma Trabalhista (Lei 13.467/17) e precedentes do TST.
@@ -428,7 +428,7 @@
     id: "familia", area: "Direito de Família",
     specialty: "Divórcio, guarda, alimentos, união estável, inventário",
     providerPref: "sambanova",
-    isCritical: false, maxTokens: 900, temperature: 0.12,
+    isCritical: false, maxTokens: 1800, temperature: 0.12,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado de Família e Sucessões com expertise em casos complexos de guarda disputada, partilha de ativos digitais e blindagem patrimonial.
@@ -522,7 +522,7 @@
     id: "consumidor", area: "Direito do Consumidor",
     specialty: "CDC, compras online, bancos, golpes, vício e fato do produto",
     providerPref: "sambanova",
-    isCritical: false, maxTokens: 900, temperature: 0.1,
+    isCritical: false, maxTokens: 1800, temperature: 0.1,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado do Consumidor com 18 anos em demandas contra bancos, fintechs e grandes varejistas. Especialista em inversão do ônus da prova e dano moral in re ipsa.
@@ -616,7 +616,7 @@
     id: "tributario", area: "Direito Tributário",
     specialty: "IR, IPTU, ICMS, multas fiscais, planejamento tributário, execução fiscal",
     providerPref: "sambanova",
-    isCritical: true, maxTokens: 1000, temperature: 0.08,
+    isCritical: true, maxTokens: 2000, temperature: 0.08,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado Tributarista com 22 anos em contencioso fiscal de alto valor, ex-auditor da RFB e especialista em tese tributária perante o STF.
@@ -713,7 +713,7 @@
     id: "empresarial", area: "Direito Empresarial",
     specialty: "Contratos comerciais, societário, recuperação judicial, M&A",
     providerPref: "sambanova",
-    isCritical: true, maxTokens: 1000, temperature: 0.1,
+    isCritical: true, maxTokens: 2000, temperature: 0.1,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado Empresarial com 20 anos em M&A, reestruturação e recuperação judicial.
@@ -810,7 +810,7 @@
     id: "imobiliario", area: "Direito Imobiliário",
     specialty: "Compra/venda, locação, usucapião, incorporação, RWA, ônus reais",
     providerPref: "sambanova",
-    isCritical: true, maxTokens: 1000, temperature: 0.08,
+    isCritical: true, maxTokens: 2000, temperature: 0.08,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado Imobiliário especializado em incorporações, RWA tokenizado e disputas possessórias.
@@ -907,7 +907,7 @@
     id: "digital", area: "Direito Digital",
     specialty: "LGPD, crimes digitais, fake news, contratos SaaS, tokens e NFTs",
     providerPref: "sambanova",
-    isCritical: false, maxTokens: 900, temperature: 0.12,
+    isCritical: false, maxTokens: 1800, temperature: 0.12,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado Digital com expertise em LGPD, criptoativos e crimes cibernéticos.
@@ -1001,7 +1001,7 @@
     id: "previdenciario", area: "Direito Previdenciário",
     specialty: "Aposentadoria, INSS, BPC/LOAS, auxílio-doença, pensão por morte",
     providerPref: "sambanova",
-    isCritical: false, maxTokens: 900, temperature: 0.1,
+    isCritical: false, maxTokens: 1800, temperature: 0.1,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado Previdenciário com expertise em benefícios negados pelo INSS e JEF.
@@ -1095,7 +1095,7 @@
     id: "ambiental", area: "Direito Ambiental",
     specialty: "Licenciamento, danos ambientais, APP, reserva legal, crimes ambientais",
     providerPref: "sambanova",
-    isCritical: true, maxTokens: 900, temperature: 0.08,
+    isCritical: true, maxTokens: 1800, temperature: 0.08,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado Ambiental com 18 anos em licenciamento e ACP ambiental.
@@ -1192,7 +1192,7 @@
     id: "constitucional", area: "Direito Constitucional",
     specialty: "Direitos fundamentais, discriminação, mandado de segurança, ADI",
     providerPref: "sambanova",
-    isCritical: false, maxTokens: 900, temperature: 0.12,
+    isCritical: false, maxTokens: 1800, temperature: 0.12,
     systemPrompts: {
       pt: buildPt(
         `Você é um Constitucionalista com expertise em litígios estratégicos e controle de constitucionalidade.
@@ -1286,7 +1286,7 @@
     id: "saude", area: "Direito à Saúde",
     specialty: "Planos de saúde, medicamentos, internação compulsória, ANS",
     providerPref: "sambanova",
-    isCritical: false, maxTokens: 900, temperature: 0.1,
+    isCritical: false, maxTokens: 1800, temperature: 0.1,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado especializado em Direito à Saúde.
@@ -1380,7 +1380,7 @@
     id: "internacional", area: "Direito Internacional",
     specialty: "Contratos internacionais, dupla nacionalidade, extradição, CISG, arbitragem",
     providerPref: "sambanova",
-    isCritical: false, maxTokens: 900, temperature: 0.12,
+    isCritical: false, maxTokens: 1800, temperature: 0.12,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado de Direito Internacional com expertise em arbitragem e contratos transfronteiriços.
@@ -1474,7 +1474,7 @@
     id: "eleitoral", area: "Direito Eleitoral",
     specialty: "Propaganda eleitoral, abuso de poder, AIJE, candidatura, cassação",
     providerPref: "sambanova",
-    isCritical: false, maxTokens: 900, temperature: 0.1,
+    isCritical: false, maxTokens: 1800, temperature: 0.1,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado Eleitoral com experiência em AIJE e cassação de mandato.
@@ -1568,7 +1568,7 @@
     id: "agrario", area: "Direito Agrário",
     specialty: "Reforma agrária, arrendamento, usucapião rural, ITR, regularização fundiária",
     providerPref: "sambanova",
-    isCritical: false, maxTokens: 900, temperature: 0.1,
+    isCritical: false, maxTokens: 1800, temperature: 0.1,
     systemPrompts: {
       pt: buildPt(
         `Você é um Advogado Agrário com expertise em conflitos fundiários e regularização de terras.
@@ -1764,7 +1764,7 @@
   area: "Contraditório Obrigatório",
   specialty: "Invalidação técnica da tese dominante",
   isCritical: false,
-  maxTokens: 1000,
+  maxTokens: 2000,
   temperature: 0.35,
   systemPrompts: {
     pt: DEVIL_BASE_PT,
--- a/src/swarm/pdfService.js
+++ b/src/swarm/pdfService.js
@@ -520,7 +520,7 @@
   agentsData.forEach((agent, i) => {
     const area         = esc(agent.area     || '');
     const specialty    = esc(agent.specialty || '');
-    const analysisText = esc((agent.analysis || '').trim().slice(0, 2500));
+    const analysisText = esc((agent.analysis || '').trim());
     const confidence   = agent.confidence != null ? `${agent.confidence}%` : '—';
     const isVeto       = analysisText.startsWith('[VETO]');
     const accColor     = isVeto ? C.redVeto : AGENT_COLORS[i % AGENT_COLORS.length];
@@ -621,14 +621,14 @@
   // Análise estratégica
   if (strategic) {
     renderer.sectionTitle('[>] ANALISE ESTRATEGICA', C.purple);
-    renderer.textBlock(strategic.slice(0, 2000), C.bgCard, C.purple, 'left');
+    renderer.textBlock(strategic, C.bgCard, C.purple, 'left');
     renderer.space(16);
   }
 }
 
 function buildDevil(renderer, devilAnalysis) {
   renderer.sectionTitle('[x] ADVOGADO DO DIABO - CONTRADITORIO', C.redVeto);
-  renderer.textBlock(devilAnalysis.slice(0, 2000), C.redDark, C.redVeto, 'left');
+  renderer.textBlock(devilAnalysis, C.redDark, C.redVeto, 'left');
   renderer.space(16);
 }
 
@@ -642,19 +642,32 @@
   renderer.sectionTitle('[~] CONFLITOS & ARBITRAGEM ENTRE ESPECIALISTAS');
 
   conflicts.slice(0, 5).forEach(cf => {
-    renderer.textBlock(`[!] ${esc(String(cf))}`, C.amberLight, C.amber, 'left', 8);
+    let cfText;
+    if (cf && typeof cf === 'object') {
+      // Objeto: { agentA, agentB, tema, severity } ou { agentAId, agentBId, tema, severity }
+      const a = cf.agentA || cf.agentAId || '';
+      const b = cf.agentB || cf.agentBId || '';
+      const tema = cf.tema || '';
+      const sev = cf.severity != null ? ` [sev: ${Number(cf.severity).toFixed(2)}]` : '';
+      const stanceA = cf.agentAStance ? `\n  >> ${cf.agentAStance}` : '';
+      const stanceB = cf.agentBStance ? `\n  >> ${cf.agentBStance}` : '';
+      cfText = `[CONFLITO] ${esc(a.toUpperCase())} x ${esc(b.toUpperCase())}: ${esc(tema)}${sev}${esc(stanceA)}${esc(stanceB)}`;
+    } else {
+      cfText = `[!] ${esc(String(cf))}`;
+    }
+    renderer.textBlock(cfText, C.amberLight, C.amber, 'left', 8);
     renderer.space(4);
   });
 
   if (revised) {
     renderer.sectionTitle('VEREDITO REVISADO PÓS-ARBITRAGEM', C.amber);
-    renderer.textBlock(revised.slice(0, 1500), C.bgCard, C.amber, 'left', 9);
+    renderer.textBlock(revised, C.bgCard, C.amber, 'left', 9);
     renderer.space(16);
   }
 
   if (convergence) {
     renderer.sectionTitle('CONVERGÊNCIA DO SWARM');
-    renderer.textBlock(convergence.slice(0, 800), C.bgPanel, C.greyMid, 'left', 8);
+    renderer.textBlock(convergence, C.bgPanel, C.greyMid, 'left', 8);
     renderer.space(16);
   }
 }
--- a/src/core/legalEngine.js
+++ b/src/core/legalEngine.js
@@ -362,7 +362,7 @@
   const raw = await callLlm(ai, {
     systemPrompt,
     userContent,
-    maxTokens: cfgAgent.maxTokens ?? 550,
+    maxTokens: cfgAgent.maxTokens ?? 1800,
     temperature: cfgAgent.temperature ?? 0.1,
     timeout,
     providerPref: cfgAgent.providerPref,
@@ -665,7 +665,7 @@
   const raw = await callLlm(ai, {
     systemPrompt: judgeSystem,
     userContent,
-    maxTokens: 1400,
+    maxTokens: 2800,
     temperature: 0.12,
     timeout,
     providerPref: 'sambanova',
@@ -706,7 +706,7 @@
   const rewritten = await callLlm(ai, {
     systemPrompt: judgeSystem,
     userContent: judgeUser + `\n\n${rewriteInstr}`,
-    maxTokens: 1400,
+    maxTokens: 2800,
     temperature: 0.10,
     timeout,
     providerPref: 'sambanova',
--- a/src/swarm/legalEngine.js
+++ b/src/swarm/legalEngine.js
@@ -178,7 +178,7 @@
   const userContent = `CASO JURÍDICO:\n${prompt.slice(0,2000)}\n\nPARECERES DOS 15 ESPECIALISTAS (para você refutar):\n${brief}\n\nExecute o Contraditório Obrigatório. ${langInstr}`;
 
   const raw = await callLlm(llmManager, devilSystem, userContent,
-    DEVIL_ADVOCATE_CONFIG.maxTokens ?? 1200, DEVIL_ADVOCATE_CONFIG.temperature ?? 0.1,
+    DEVIL_ADVOCATE_CONFIG.maxTokens ?? 2400, DEVIL_ADVOCATE_CONFIG.temperature ?? 0.1,
     60, 'sambanova');
 
   const text = raw ?? '⚠️ Advogado do Diabo indisponível — Contraditório não processado.';
--- a/src/swarm/convergenceEngine.js
+++ b/src/swarm/convergenceEngine.js
@@ -374,7 +374,7 @@
       llmManager.complete({
         systemPrompt,
         userContent,
-        maxTokens: 1800,
+        maxTokens: 3600,
         temperature: 0.10,
         timeout: Math.round(timeout),
         providerPref: 'sambanova',
--- a/src/swarm/strategicAgent.js
+++ b/src/swarm/strategicAgent.js
@@ -24,7 +24,7 @@
   specialty: 'Foro, tipo de ação, tutela, custo/benefício, tática de instâncias',
   providerPref: 'sambanova',
   isCritical: true,
-  maxTokens: 800,
+  maxTokens: 1600,
   temperature: 0.10,
   runAfterSwarm: true,
   systemPrompts: {}, // preenchido abaixo
